#include "main.h"

/**
 *_isupper - this function
 *@c:this is paramater for upper function
 *Return:itn
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
